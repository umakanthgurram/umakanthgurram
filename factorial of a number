#include<stdio.h>
int main(){
    int n,i,factorial;
    printf("enter the number for which factorial required ");
    scanf("%d",&n);
    factorial=1;
    for(i=1;i<=n;i++){
        factorial *= i;
    }
    printf("%d",factorial);
    return 0;
}
