
#include <stdio.h>

int main() {
    int i,n,sum;
    printf("enter the number up to which cubes are required:");
    scanf("%d",&n);
    sum=0;
    for(i=2;i<=n;i=i+2){
        sum += i*i*i;
    }
    printf("%d",sum);
    return 0;
}
